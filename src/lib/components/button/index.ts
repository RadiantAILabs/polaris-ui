import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants
} from './Button.svelte';

export {
	Root,
	type ButtonProps,
	//
	Root as Button,
	buttonVariants,
	type ButtonSize,
	type ButtonVariant
};
